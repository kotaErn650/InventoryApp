<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InventoryApp.Views.ProductsPage"
             Title="ProductsPage">


    <VerticalStackLayout Padding="20">

        <Button
            Text="Nuevo Producto!"
            Command="{Binding NewCommand}" />

        <CollectionView
            ItemsSource="{Binding Products}">

            <CollectionView.ItemTemplate>

                <DataTemplate>

                    <Border Margin="5">

                        <VerticalStackLayout>

                            <Label Text="{Binding Nombre}" FontSize="20"/>

                            <Label Text="{Binding Precio}"/>

                            <Label Text="{Binding Stock}"/>

                            <Button
                                Text="Deshabilitar"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DisableCommand}"
                                CommandParameter="{Binding .}"/>




                        </VerticalStackLayout>

                    </Border>

                </DataTemplate>

            </CollectionView.ItemTemplate>
            
        </CollectionView>


        <Button 
            Text="HOLA" 
            FontSize="20"
            TextColor="AliceBlue"
            Clicked="OnHolaClicked"/>

    </VerticalStackLayout>
</ContentPage>